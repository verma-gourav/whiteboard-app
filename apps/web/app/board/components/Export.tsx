import { DownloadIcon } from "@/components/icons";
import { Button } from "@/components/ui";

const Export = () => {
  return <Button variant="ghost" size="md" icon={<DownloadIcon />} />;
};

export default Export;
